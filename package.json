{
  "name": "auto-merge-or-pr",
  "version": "0.1.2",
  "private": true,
  "description": "GitHub Action to create pull requests",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc",
    "dev": "yarn install",
    "release": "yarn && yarn run dev && yarn run build && rimraf ./node_modules && yarn install --production"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rgmills/auto-merge-or-pr.git"
  },
  "author": "Robert Mills",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^12.0.4",
    "rimraf": "2.6.2",
    "typescript": "3.6.3"
  }
}